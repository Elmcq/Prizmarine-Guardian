# Dependencies
node_modules/

# WhatsApp session cache (LocalAuth)
.wwebjs_auth/

# Runtime logs
logs/

# Runtime data (contains user PII — phone numbers, warnings, bans, incidents)
# NEVER commit this folder EXCEPT badwords.json (keyword lists, no PII)
data/*
!data/badwords.json

# Local environment / secrets
.env

# OS / editor cruft
.DS_Store
*.log

# ZCode session artifacts (tooling, not part of the bot)
.zcode/
